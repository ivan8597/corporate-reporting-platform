version: 1
services:
  - type: web
    name: corporate-reporting
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    plan: free
    envVars:
      - key: DB_TYPE
        value: sqlite
      - key: LOG_LEVEL
        value: INFO
    autoDeploy: true